create_shortcut_buildout.ps1
============================
Use PowerShell's WScript.Shell object's CreateShortcut() interface to place a Plone shortcut on the desktop for machines which used Buildout to install Plone