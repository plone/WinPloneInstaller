start_plone.ps1
===============
start_plone.ps1 is called after installation completes if the user has selected "Start Plone After Installation."
It simply starts bash as its own process so Plone can run within and the installer can close.